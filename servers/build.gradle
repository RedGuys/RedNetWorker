plugins {
    id 'java'
}

group 'ru.redguy.rednetworker'
version '1.0.0'

repositories {
    mavenCentral()
}

jar {
    from { configurations.runtime.collect { it.isDirectory() ? it : zipTree(it) } }
}

dependencies {
   compile project(":servers:tftp")
}
